DATABASE_URL="postgresql://postgres:postgres@localhost:5432/meedya?schema=public"
ADMIN_USERNAME="admin"
# Create with: node -e "const bcrypt=require('bcryptjs'); console.log(bcrypt.hashSync('replace-me',12))"
ADMIN_PASSWORD_HASH="$2a$12$replace_with_bcrypt_hash"
SESSION_SECRET="replace-with-32-char-secret"
